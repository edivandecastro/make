import React from "react";
import TopBar from '../Topbar';

const HumanResources = ({content_type}) => (
  <div>
    <TopBar modCode="M01" />
    <div className="page-content">
      DASHBOARD DE RECURSOS HUMANOS
    </div>
  </div>
);

export default HumanResources;
